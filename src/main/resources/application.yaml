server:
  port: 8092
  servlet:
    context-path: /elasticsearch

#   db configs
spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/sugea
    username: root
    password: root
  jpa:
    show-sql: false
    properties:
      hibernate.format_sql: false


# elsaticsearch configs
  elasticsearch:
    uris: http://localhost:9200
    username: elastic
    password: changeme

  